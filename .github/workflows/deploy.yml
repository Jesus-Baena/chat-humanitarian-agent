name: Build and Push Docker Image

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: jesus-baena/chat-humanitarian-agent

jobs:
  build:
    runs-on: ubuntu-latest  # Use GitHub cloud runners for building
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Verify build arguments (diagnostic)
        run: |
          echo "🔍 Checking if secrets are available for build..."
          echo "NUXT_UI_PRO_LICENSE: ${{ secrets.NUXT_UI_PRO_LICENSE != '' && '✅ Set' || '❌ Missing' }}"
          echo "NUXT_PUBLIC_SITE_URL: ${{ secrets.NUXT_PUBLIC_SITE_URL != '' && '✅ Set' || '❌ Missing' }}"
          echo "NUXT_PUBLIC_SUPABASE_URL: ${{ secrets.NUXT_PUBLIC_SUPABASE_URL != '' && '✅ Set' || '❌ Missing' }}"
          echo "NUXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NUXT_PUBLIC_SUPABASE_ANON_KEY != '' && '✅ Set' || '❌ Missing' }}"
          echo "NUXT_PUBLIC_FLOWISE_URL: ${{ secrets.NUXT_PUBLIC_FLOWISE_URL != '' && '✅ Set' || '❌ Missing' }}"
          echo "NUXT_PUBLIC_FLOWISE_API_KEY: ${{ secrets.NUXT_PUBLIC_FLOWISE_API_KEY != '' && '✅ Set' || '❌ Missing' }}"

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64
          build-args: |
            NUXT_UI_PRO_LICENSE=${{ secrets.NUXT_UI_PRO_LICENSE }}
            NUXT_PUBLIC_SITE_URL=${{ secrets.NUXT_PUBLIC_SITE_URL }}
            NUXT_PUBLIC_SUPABASE_URL=${{ secrets.NUXT_PUBLIC_SUPABASE_URL }}
            NUXT_PUBLIC_SUPABASE_ANON_KEY=${{ secrets.NUXT_PUBLIC_SUPABASE_ANON_KEY }}
            NUXT_PUBLIC_FLOWISE_URL=${{ secrets.NUXT_PUBLIC_FLOWISE_URL }}
            NUXT_PUBLIC_FLOWISE_API_KEY=${{ secrets.NUXT_PUBLIC_FLOWISE_API_KEY }}

      - name: Build Summary
        if: success()
        run: |
          echo "✅ Successfully built and pushed Docker image"
          echo "🐳 Image: ghcr.io/jesus-baena/chat-humanitarian-agent:latest"
          echo "📦 To deploy, run: ./manual-deploy.sh"
          echo "🔗 Or deploy manually on Swarm host:"
          echo "   sudo docker service update --image ghcr.io/jesus-baena/chat-humanitarian-agent:latest --force web_chat"
